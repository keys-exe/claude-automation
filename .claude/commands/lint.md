---
description: E10 doc-lint over the Standards — a cut failing lint does not ship
---
Run `pe lint`.

Group the findings by rule. For count mismatches, name the string, the declared count and the
measured one. Do not edit any string body to make a count match — the count is corrected to
the block, unless the block itself has drifted from what was tested.
