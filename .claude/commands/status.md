---
description: Every build gate at once — doc-lint, coverage, act map, ledger
---
Run `pe status` in the current build directory and report what it says.

If anything fails, name the failing gate, the section it comes from, and the single next
action. Do not offer to proceed past a failing gate.
