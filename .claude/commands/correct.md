---
description: Apply the §34 correction protocol to a flagged beat
argument-hint: <BEAT-ID> <what is wrong>
---
Use the `correction` skill.

Return only the corrected block for $1, then one line confirming the fix, then the global
scan result, then the retroactive reissue list, then the Pending Amendment entry. Nothing else.
